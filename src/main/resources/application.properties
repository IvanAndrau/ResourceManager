spring.application.name=ResourceManagerWebTask
# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/resourcesdb
spring.datasource.username=adminuser
spring.datasource.password=123qweasd

# Hibernate Configuration     create
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# For ddl-auto:  create - drops and recreates the schema every time the application starts